# Origin of the swifty_chat Go backend (see swifty_chat/config.json).
VITE_API_URL=http://localhost:8000

# Optional. Defaults to VITE_API_URL with the http scheme swapped for ws.
# Set this only when the WebSocket endpoint is proxied to a different origin.
# VITE_WS_URL=ws://localhost:8000
